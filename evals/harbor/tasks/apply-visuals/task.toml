schema_version = "1.4"
artifacts = []

[task]
name = "presentations/apply-visuals"
version = "1.0.0"
description = "Apply the VISUALS.md additions to one slide, leave the rest and content/ alone, build pass 2"
keywords = ["skill", "apply-visuals"]
[[task.authors]]
name = "Cordero Core"

[metadata]

[verifier]
timeout_sec = 120.0
collect = []

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []
skills_dir = "/skills"

[environment.env]

[solution.env]
