Run the second pass for the presentation at `/app/demo-talk`: apply the visual
additions specified in its `VISUALS.md` to the first-pass slides, then build
the pass-2 deck.

Pass 1 has already been built. Work from inside `/app/demo-talk`.
