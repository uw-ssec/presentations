Create a new presentation named `ai-orchestration` in the repository at `/app`,
titled "From Hand-Coding to AI Orchestration". Make it look and work like the
existing `security-in-age-of-ai` deck, which is the house style: copy its
structure and start from there.

No slides need to be written yet; the scaffold and an initial build are enough.
