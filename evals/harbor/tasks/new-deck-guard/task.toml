schema_version = "1.4"
artifacts = []

[task]
name = "presentations/new-deck-guard"
version = "1.0.0"
description = "Guard: asked to copy the legacy Reveal.js deck, the agent must still scaffold a fragment deck"
keywords = ["skill", "new-deck"]
[[task.authors]]
name = "Cordero Core"

[metadata]

[verifier]
timeout_sec = 120.0
collect = []

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []
skills_dir = "/skills"

[environment.env]

[solution.env]
