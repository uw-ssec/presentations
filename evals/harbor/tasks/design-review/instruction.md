Run a design review of the SSEC-brand presentation at `/app/demo-talk`, over
the first-pass fragments in `content/`, and write the report to
`/app/demo-talk/design-review.md` in the skill's output format (a compliant
line, or a Violations list naming each slide and the fix).

Do not fix the slides; this is a review. The brand is the default (SSEC).
