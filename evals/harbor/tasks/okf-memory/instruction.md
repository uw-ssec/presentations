Record in the project memory of the repository at `/app` the reason new decks
default to uw-slides fragments: one fragment per slide lets an agent edit a
single slide without touching the rest. This refines the existing deck-format
decision rather than replacing it.

Use the actor token `claude-code:test-model`.
