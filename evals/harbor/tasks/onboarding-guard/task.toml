schema_version = "1.4"
artifacts = []

[task]
name = "presentations/onboarding-guard"
version = "1.0.0"
description = "Keep the tour read-only when the newcomer asks for a quick fix along the way"
keywords = ["skill", "onboarding", "guard"]
[[task.authors]]
name = "Cordero Core"

[metadata]

[verifier]
timeout_sec = 120.0
collect = []

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []
skills_dir = "/skills"

[environment.env]

[solution.env]
