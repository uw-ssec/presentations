Scaffold a new presentation named `rse-meetup-2026` in the repository at `/app`
using the default brand, then build it once so `rse-meetup-2026/build/index.html`
exists.

The user has already answered the questions the workflow would ask:

- Brand: the default (SSEC).
- Presentation title: "Reproducible slides from HTML fragments".
- No slides need to be written yet; the scaffold and an initial build are enough.

A skill that describes exactly how to do this is available to you. Its paths are
relative to the repository root, `/app`.
