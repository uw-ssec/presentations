Run an accessibility check (WCAG 2.1 AA) of the presentation at
`/app/demo-talk`, over the first-pass fragments in `content/`, and write the
report to `/app/demo-talk/accessibility-report.md` in the skill's output
format, naming each failing slide and what fails.

Do not fix the slides; this is a check. The brand is the default (SSEC).
